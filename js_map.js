var vg_1 = "daily_cost_bar_chart.vg.json";
vg.embed("#bar_chart", vg_1, function(error, result) {
  // Callback receiving the View instance and parsed Vega spec
  // result.view is the View, which resides under the '#vis' element
});
